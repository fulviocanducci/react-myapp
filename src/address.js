import React, { Component } from "react";

class Address extends Component {
    render() {
        return (
            <div>Endereços carregado ...</div>
        );
    }
}

export default Address;